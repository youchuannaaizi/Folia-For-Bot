<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>xyz.ycmc.mc.plugins</groupId>
    <artifactId>yc-bot</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>
    <name>YCBot</name>
    <description>Paper 1.21.5 Java 21 Plugin</description>

    <!-- 根据文档自动获取最新版 -->
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Java 21 配置 -->
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <!-- 通过 PaperMC API 获取的最新版本 -->
        <paper.minecraft.version>1.21.5</paper.minecraft.version>
        <paper.build.number>123</paper.build.number> <!-- 示例值，需动态获取 -->
        <paper.version>${paper.minecraft.version}-R0.1-SNAPSHOT</paper.version>
    </properties>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <!-- 发布到 GitHub Packages -->
    <distributionManagement>
        <repository>
            <id>github</id>
            <name>GitHub youchuannaaizi Apache Maven Packages</name>
            <url>https://maven.pkg.github.com/youchuannaaizi/Folia-For-Bot.git</url>
        </repository>
    </distributionManagement>

    <dependencies>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>${paper.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Java 21 编译配置 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <compilerArgs>--enable-preview</compilerArgs>
                    <release>21</release>
                </configuration>
            </plugin>
            <!-- 确保包含部署插件 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>3.1.1</version>
            </plugin>
        </plugins>
    </build>
</project>